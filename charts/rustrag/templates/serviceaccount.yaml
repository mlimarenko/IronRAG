{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rustrag.serviceAccountName" . }}
  labels:
    {{- include "rustrag.labels" . | nindent 4 }}
{{- end }}
