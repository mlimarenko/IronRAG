{{- if eq .Values.startup.mode "startup_job" }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "rustrag.fullname" . }}-startup-wait
  labels:
    {{- include "rustrag.labels" . | nindent 4 }}
rules:
  - apiGroups:
      - batch
    resources:
      - jobs
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - apps
    resources:
      - deployments
    verbs:
      - get
      - list
      - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "rustrag.fullname" . }}-startup-wait
  labels:
    {{- include "rustrag.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "rustrag.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "rustrag.fullname" . }}-startup-wait
{{- end }}
