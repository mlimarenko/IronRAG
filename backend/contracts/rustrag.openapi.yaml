openapi: 3.1.0
info:
  title: RustRAG API
  version: 0.1.0
  description: |
    Canonical API contract for the current RustRAG backend surface.

    This contract tracks the active public Axum routes mounted from `backend/src/interfaces/http`
    and is rewritten at request time with proxy-safe server origins for the current entrypoint.
servers:
  - url: /v1
    description: Relative API root
security:
  - bearerAuth: []
tags:
  - name: system
  - name: catalog
  - name: iam
  - name: ai
  - name: ingest
  - name: search
  - name: query
  - name: billing
  - name: ops
  - name: audit
  - name: automation
  - name: ui-auth
  - name: ui-shell
  - name: workspaces
  - name: ingestion
  - name: documents
  - name: content
  - name: product
  - name: graph
x-greenfield-scaffold:
  canonicalPathPrefixes:
    - /v1/catalog
    - /v1/iam
    - /v1/ai
    - /v1/content
    - /v1/ingest
    - /v1/graph
    - /v1/search
    - /v1/query
    - /v1/billing
    - /v1/ops
    - /v1/audit
    - /v1/mcp
  forbiddenLegacyVocabulary:
    - runtime_
    - ui_
    - mcp_
  seededAiCatalog:
    providers: 3
    models: 7
    prices: 12
  freshBootstrapDiscovery:
    - /v1/openapi/rustrag.openapi.yaml
    - /v1/iam/bootstrap/claim
  scaffoldStatus: canonical-cutover
paths:
  /v1/health:
    get:
      tags: [system]
      summary: Health check
      operationId: getHealth
      security: []
      responses:
        "200":
          description: Service health
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
  /v1/ready:
    get:
      tags: [system]
      summary: Readiness check
      operationId: getReadiness
      security: []
      responses:
        "200":
          description: Readiness probe result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessResponse"
  /v1/version:
    get:
      tags: [system]
      summary: Service version
      operationId: getVersion
      security: []
      responses:
        "200":
          description: Service version metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VersionResponse"
  /v1/openapi/rustrag.openapi.yaml:
    get:
      tags: [system]
      summary: OpenAPI contract discovery
      operationId: getOpenApiContract
      security: []
      responses:
        "200":
          description: Rendered OpenAPI contract for the current public entrypoint
          content:
            application/yaml:
              schema:
                type: string
  /v1/iam/bootstrap/claim:
    post:
      tags: [iam]
      summary: Claim the first administrator for a fresh deployment
      operationId: postBootstrapClaim
      security: []
      description: |
        One-time bootstrap route for destructive fresh deployments. This route is expected to
        succeed exactly once, create the first `iam_principal` plus `iam_user`, and emit one
        canonical `audit_event`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BootstrapClaimRequest"
      responses:
        "200":
          description: Bootstrap claim succeeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BootstrapClaimResponse"
        "400":
          description: Invalid bootstrap payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Invalid bootstrap secret
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "403":
          description: Bootstrap claim disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "409":
          description: Bootstrap was already claimed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/mcp:
    post:
      tags: [automation]
      summary: MCP JSON-RPC endpoint for canonical agent memory flows
      operationId: postMcpRequest
      description: |
        Bearer-token authenticated JSON-RPC transport for discovery, search, read, and document
        mutation tools. Tool visibility and callable actions are derived from the authenticated
        principal's typed grants.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/McpJsonRpcRequest"
      responses:
        "200":
          description: JSON-RPC response envelope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/McpJsonRpcResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/mcp/capabilities:
    get:
      tags: [automation]
      summary: Return the derived MCP capability snapshot for the current principal
      operationId: getMcpCapabilities
      responses:
        "200":
          description: Derived capability snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/McpCapabilitySnapshot"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/catalog/workspaces:
    get:
      tags: [catalog]
      summary: List visible workspaces
      operationId: listCatalogWorkspaces
      responses:
        "200":
          description: Visible workspaces for the authenticated principal
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CatalogWorkspaceResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
    post:
      tags: [catalog]
      summary: Create a workspace
      operationId: createCatalogWorkspace
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCatalogWorkspaceRequest"
      responses:
        "200":
          description: Created workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CatalogWorkspaceResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "409":
          description: Workspace slug conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/catalog/workspaces/{workspaceId}:
    get:
      tags: [catalog]
      summary: Get one visible workspace
      operationId: getCatalogWorkspace
      parameters:
        - in: path
          name: workspaceId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Workspace details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CatalogWorkspaceResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Workspace not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/catalog/workspaces/{workspaceId}/libraries:
    get:
      tags: [catalog]
      summary: List visible libraries in one workspace
      operationId: listCatalogLibraries
      parameters:
        - in: path
          name: workspaceId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Libraries visible in the workspace
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CatalogLibraryResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
    post:
      tags: [catalog]
      summary: Create a library in one workspace
      operationId: createCatalogLibrary
      parameters:
        - in: path
          name: workspaceId
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCatalogLibraryRequest"
      responses:
        "200":
          description: Created library
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CatalogLibraryResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Workspace not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "409":
          description: Library slug conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/catalog/libraries/{libraryId}:
    get:
      tags: [catalog]
      summary: Get one visible library
      operationId: getCatalogLibrary
      parameters:
        - in: path
          name: libraryId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Library details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CatalogLibraryResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Library not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/iam/session/login:
    post:
      tags: [iam]
      summary: Authenticate a browser session
      operationId: loginIamSession
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginSessionRequest"
      responses:
        "200":
          description: Authenticated session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionResponse"
        "400":
          description: Invalid payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/iam/session:
    get:
      tags: [iam]
      summary: Resolve the current browser session
      operationId: getIamSession
      responses:
        "200":
          description: Current browser session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionResponse"
        "401":
          description: Missing or invalid browser session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/iam/session/logout:
    post:
      tags: [iam]
      summary: Revoke the current browser session
      operationId: logoutIamSession
      responses:
        "204":
          description: Session revoked
        "401":
          description: Missing or invalid browser session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/iam/me:
    get:
      tags: [iam]
      summary: Resolve the current principal, memberships, and effective grants
      operationId: getIamMe
      responses:
        "200":
          description: Canonical principal view
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeResponse"
        "401":
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/iam/tokens:
    get:
      tags: [iam]
      summary: List visible API tokens
      operationId: listIamTokens
      parameters:
        - in: query
          name: workspaceId
          required: false
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: API tokens
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TokenResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
    post:
      tags: [iam]
      summary: Mint a canonical API token
      operationId: mintIamToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MintTokenRequest"
      responses:
        "200":
          description: Minted token and persisted API token record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MintTokenResponse"
        "400":
          description: Invalid token payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/iam/tokens/{tokenPrincipalId}/revoke:
    post:
      tags: [iam]
      summary: Revoke one API token principal
      operationId: revokeIamToken
      parameters:
        - in: path
          name: tokenPrincipalId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Token revoked
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Token not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/iam/grants:
    get:
      tags: [iam]
      summary: List effective grants for one principal
      operationId: listIamGrants
      parameters:
        - in: query
          name: principalId
          required: false
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Effective grants for the requested principal
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GrantResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
    post:
      tags: [iam]
      summary: Create a typed grant
      operationId: createIamGrant
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGrantRequest"
      responses:
        "200":
          description: Grant created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrantResponse"
        "400":
          description: Invalid grant payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/iam/grants/{grantId}:
    delete:
      tags: [iam]
      summary: Revoke one typed grant
      operationId: revokeIamGrant
      parameters:
        - in: path
          name: grantId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Grant revoked
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Grant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ai/providers:
    get:
      tags: [ai]
      summary: List seeded AI providers
      operationId: listAiProviders
      responses:
        "200":
          description: AI provider catalog entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProviderCatalogEntryResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ai/models:
    get:
      tags: [ai]
      summary: List seeded AI models
      operationId: listAiModels
      parameters:
        - in: query
          name: providerCatalogId
          required: false
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: AI model catalog entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ModelCatalogEntryResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ai/model-presets:
    get:
      tags: [ai]
      summary: List workspace model presets
      operationId: listAiModelPresets
      parameters:
        - in: query
          name: workspaceId
          required: false
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Workspace model presets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ModelPresetResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ai/prices:
    get:
      tags: [ai]
      summary: List seeded AI prices
      operationId: listAiPrices
      parameters:
        - in: query
          name: modelCatalogId
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: workspaceId
          required: false
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: AI price catalog entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PriceCatalogEntryResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ai/credentials:
    get:
      tags: [ai]
      summary: List visible provider credentials for one workspace
      operationId: listAiCredentials
      parameters:
        - in: query
          name: workspaceId
          required: false
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Provider credentials
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProviderCredentialResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
    post:
      tags: [ai]
      summary: Create a provider credential
      operationId: createAiCredential
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProviderCredentialRequest"
      responses:
        "200":
          description: Provider credential created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProviderCredentialResponse"
        "400":
          description: Invalid provider credential payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ai/library-bindings/{libraryId}:
    get:
      tags: [ai]
      summary: List active model bindings for one library
      operationId: listAiLibraryBindings
      parameters:
        - in: path
          name: libraryId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Active library model bindings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LibraryModelBindingResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ai/library-bindings:
    post:
      tags: [ai]
      summary: Create or replace the active binding for one library purpose
      operationId: createAiLibraryBinding
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLibraryBindingRequest"
      responses:
        "200":
          description: Library model binding created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LibraryModelBindingResponse"
        "400":
          description: Invalid library binding payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ai/library-bindings/{bindingId}/validate:
    post:
      tags: [ai]
      summary: Trigger one library-binding validation row
      operationId: validateAiLibraryBinding
      parameters:
        - in: path
          name: bindingId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Binding validation request accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BindingValidationResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Binding not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/content/documents:
    get:
      tags: [content]
      summary: List canonical logical documents for one library
      operationId: listContentDocuments
      parameters:
        - in: query
          name: libraryId
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: includeDeleted
          required: false
          schema:
            type: boolean
      responses:
        "200":
          description: Logical documents in the requested library
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ContentDocumentDetailResponse"
        "400":
          description: Invalid document listing request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
    post:
      tags: [content]
      summary: Create a logical document and accepted upload mutation
      operationId: createContentDocument
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateContentDocumentRequest"
      responses:
        "200":
          description: Created logical document and accepted mutation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateContentDocumentResponse"
        "400":
          description: Invalid document creation request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/content/documents/upload:
    post:
      tags: [content]
      summary: Upload one document revision directly into the canonical content pipeline
      operationId: uploadContentDocument
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/UploadContentDocumentRequest"
      responses:
        "200":
          description: Created logical document and accepted upload mutation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateContentDocumentResponse"
        "400":
          description: Invalid upload request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Library not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/content/documents/{documentId}:
    get:
      tags: [content]
      summary: Get one canonical logical document
      operationId: getContentDocument
      parameters:
        - in: path
          name: documentId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Canonical logical document detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentDocumentDetailResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Document not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
    delete:
      tags: [content]
      summary: Delete one logical document through a canonical mutation
      operationId: deleteContentDocument
      parameters:
        - in: path
          name: documentId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Accepted and applied document deletion mutation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentMutationDetailResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Document not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/content/documents/{documentId}/append:
    post:
      tags: [content]
      summary: Append plain text to one logical document
      operationId: appendContentDocument
      parameters:
        - in: path
          name: documentId
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AppendContentDocumentRequest"
      responses:
        "200":
          description: Accepted append mutation with per-item state and linked job
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentMutationDetailResponse"
        "400":
          description: Invalid append request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Document not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/content/documents/{documentId}/replace:
    post:
      tags: [content]
      summary: Replace the active document revision with a new uploaded file
      operationId: replaceContentDocument
      parameters:
        - in: path
          name: documentId
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ReplaceContentDocumentRequest"
      responses:
        "200":
          description: Accepted replacement mutation with per-item state and linked job
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentMutationDetailResponse"
        "400":
          description: Invalid replacement request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Document not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/content/documents/{documentId}/revisions:
    get:
      tags: [content]
      summary: List immutable revisions for one logical document
      operationId: listContentRevisions
      parameters:
        - in: path
          name: documentId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Immutable content revisions for one document
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ContentRevision"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Document not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/content/mutations:
    get:
      tags: [content]
      summary: List canonical content mutations for one library
      operationId: listContentMutations
      parameters:
        - in: query
          name: libraryId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Canonical content mutations in the requested library
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ContentMutationDetailResponse"
        "400":
          description: Invalid mutation listing request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
    post:
      tags: [content]
      summary: Create a canonical content mutation
      operationId: createContentMutation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateContentMutationRequest"
      responses:
        "200":
          description: Accepted mutation with per-item state and linked job
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentMutationDetailResponse"
        "400":
          description: Invalid mutation request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/content/mutations/{mutationId}:
    get:
      tags: [content]
      summary: Get one canonical content mutation and items
      operationId: getContentMutation
      parameters:
        - in: path
          name: mutationId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Canonical content mutation detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentMutationDetailResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Mutation not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ingest/jobs:
    get:
      tags: [ingest]
      summary: List canonical ingest jobs for one library
      operationId: listIngestJobs
      parameters:
        - in: query
          name: workspaceId
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: libraryId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Canonical ingest jobs in one library
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/IngestJob"
        "400":
          description: Invalid ingest job query
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ingest/jobs/{jobId}:
    get:
      tags: [ingest]
      summary: Get one canonical ingest job
      operationId: getIngestJob
      parameters:
        - in: path
          name: jobId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Canonical ingest job detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IngestJob"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Ingest job not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ingest/attempts/{attemptId}:
    get:
      tags: [ingest]
      summary: Get one canonical ingest attempt
      operationId: getIngestAttempt
      parameters:
        - in: path
          name: attemptId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Canonical ingest attempt detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IngestAttempt"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Ingest attempt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ingest/attempts/{attemptId}/stages:
    get:
      tags: [ingest]
      summary: List ordered stage events for one ingest attempt
      operationId: listIngestStageEvents
      parameters:
        - in: path
          name: attemptId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Ordered stage events for the requested attempt
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/IngestStageEvent"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Ingest attempt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/graph/libraries/{libraryId}/projection:
    get:
      tags: [graph]
      summary: Get the active canonical graph projection for one library
      operationId: getGraphProjection
      parameters:
        - in: path
          name: libraryId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Active graph projection
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphProjectionDetailResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Graph projection not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/graph/libraries/{libraryId}/nodes:
    get:
      tags: [graph]
      summary: List canonical graph nodes for the active library projection
      operationId: listGraphNodes
      parameters:
        - in: path
          name: libraryId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Canonical graph nodes for the active projection
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GraphNode"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Graph projection not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/graph/libraries/{libraryId}/edges:
    get:
      tags: [graph]
      summary: List canonical graph edges for the active library projection
      operationId: listGraphEdges
      parameters:
        - in: path
          name: libraryId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Canonical graph edges for the active projection
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GraphEdge"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "404":
          description: Graph projection not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/search/documents:
    post:
      tags: [search]
      summary: Search readable documents in one library
      operationId: searchDocuments
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SearchRequest"
      responses:
        "200":
          description: Ranked document hits
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SearchHit"
        "400":
          description: Invalid search request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/query/conversations:
    get:
      tags: [query]
      summary: List canonical conversations for one library
      operationId: listQueryConversations
      parameters:
        - in: query
          name: libraryId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Visible conversations ordered by most recent activity
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/QueryConversation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [query]
      summary: Create one canonical conversation
      operationId: createQueryConversation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateQueryConversationRequest"
      responses:
        "200":
          description: Conversation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryConversation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /v1/query/conversations/{conversationId}:
    get:
      tags: [query]
      summary: Get canonical conversation detail
      operationId: getQueryConversation
      parameters:
        - in: path
          name: conversationId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Conversation detail with turns and executions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryConversationDetailResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /v1/query/conversations/{conversationId}/turns:
    post:
      tags: [query]
      summary: Submit a user turn and execute one grounded answer
      operationId: createQueryTurn
      parameters:
        - in: path
          name: conversationId
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateQueryTurnRequest"
      responses:
        "200":
          description: User turn accepted and grounded answer completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryTurnExecutionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /v1/query/executions/{executionId}:
    get:
      tags: [query]
      summary: Get canonical grounded execution detail
      operationId: getQueryExecution
      parameters:
        - in: path
          name: executionId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Execution detail with typed references
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryExecutionDetailResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /v1/billing/provider-calls:
    get:
      tags: [billing]
      summary: List canonical provider calls for one library
      operationId: listBillingProviderCalls
      parameters:
        - in: query
          name: libraryId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Provider calls attributed to canonical executions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BillingProviderCall"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /v1/billing/charges:
    get:
      tags: [billing]
      summary: List canonical priced charges for one library
      operationId: listBillingCharges
      parameters:
        - in: query
          name: libraryId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Charges derived from canonical provider calls
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BillingCharge"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /v1/billing/executions/{executionKind}/{executionId}:
    get:
      tags: [billing]
      summary: Get canonical execution cost rollup
      operationId: getBillingExecutionCost
      parameters:
        - in: path
          name: executionKind
          required: true
          schema:
            type: string
        - in: path
          name: executionId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Cost rollup for one execution owner
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingExecutionCost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /v1/audit/events:
    get:
      tags: [audit]
      summary: List canonical audit events
      operationId: listAuditEvents
      parameters:
        - in: query
          name: actorPrincipalId
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: workspaceId
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: libraryId
          required: false
          schema:
            type: string
            format: uuid
        - in: query
          name: internal
          required: false
          schema:
            type: boolean
      responses:
        "200":
          description: Canonical audit events with visible subjects
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AuditEventResponse"
        "401":
          description: Missing or insufficient authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
        "403":
          description: Internal view requires system administrator access
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorBody"
  /v1/ui/auth/login:
    post:
      tags: [ui-auth]
      summary: Sign in with login and password for a browser UI session
      operationId: uiLogin
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UiLoginRequest"
      responses:
        "200":
          description: Current UI session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UiSessionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/ui/auth/session:
    get:
      tags: [ui-auth]
      summary: Get the current authenticated UI session
      operationId: getUiSession
      security:
        - uiSessionCookie: []
      responses:
        "200":
          description: Current UI session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UiSessionResponse"
        "204":
          description: No active UI session
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/ui/auth/logout:
    post:
      tags: [ui-auth]
      summary: End the current browser UI session
      operationId: uiLogout
      security:
        - uiSessionCookie: []
      responses:
        "200":
          description: Logout acknowledgement
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UiOkResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/ui/context:
    get:
      tags: [ui-shell]
      summary: Get the current authenticated shell context
      operationId: getUiContext
      security:
        - uiSessionCookie: []
      responses:
        "200":
          description: Active workspace, library, locale, and user chip data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UiShellContextResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    put:
      tags: [ui-shell]
      summary: Update the active workspace, library, or locale for the UI session
      operationId: updateUiContext
      security:
        - uiSessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UiUpdateContextRequest"
      responses:
        "200":
          description: Updated shell context
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UiShellContextResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/ui/workspaces:
    get:
      tags: [ui-shell]
      summary: List workspaces visible to the current UI session
      operationId: listUiWorkspaces
      security:
        - uiSessionCookie: []
      responses:
        "200":
          description: Visible workspace options
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UiWorkspaceOption"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [ui-shell]
      summary: Create a workspace from the authenticated shell
      operationId: createUiWorkspace
      security:
        - uiSessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UiCreateWorkspaceRequest"
      responses:
        "200":
          description: Created workspace option
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UiWorkspaceOption"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/ui/libraries:
    get:
      tags: [ui-shell]
      summary: List libraries visible in the selected workspace
      operationId: listUiLibraries
      security:
        - uiSessionCookie: []
      parameters:
        - $ref: "#/components/parameters/workspaceId"
      responses:
        "200":
          description: Visible library options
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UiLibraryOption"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [ui-shell]
      summary: Create a library inside the selected workspace
      operationId: createUiLibrary
      security:
        - uiSessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UiCreateLibraryRequest"
      responses:
        "200":
          description: Created library option
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UiLibraryOption"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/workspaces:
    get:
      tags: [workspaces]
      summary: List workspaces
      operationId: listWorkspaces
      security: []
      responses:
        "200":
          description: Workspace list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WorkspaceSummary"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [workspaces]
      summary: Create workspace
      operationId: createWorkspace
      description: Requires `workspace:admin` scope or an `instance_admin` token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkspaceRequest"
      responses:
        "200":
          description: Created workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceSummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/workspaces/{id}/governance:
    get:
      tags: [workspaces, usage]
      summary: Get workspace governance summary
      operationId: getWorkspaceGovernance
      description: Requires `workspace:admin`, `usage:read`, `providers:admin`, or an `instance_admin` token. Workspace-scoped tokens may only access their own workspace.
      parameters:
        - $ref: "#/components/parameters/id"
      responses:
        "200":
          description: Workspace governance summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceGovernanceSummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/sources:
    get:
      tags: [ingestion]
      summary: List sources
      operationId: listSources
      security: []
      parameters:
        - $ref: "#/components/parameters/projectIdOptional"
      responses:
        "200":
          description: Source list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SourceSummary"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [ingestion]
      summary: Create source
      operationId: createSource
      description: Requires `documents:write`, `workspace:admin`, or an `instance_admin` token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSourceRequest"
      responses:
        "200":
          description: Created source
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SourceSummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/ingestion-jobs:
    get:
      tags: [ingestion]
      summary: List ingestion jobs
      operationId: listIngestionJobs
      security: []
      parameters:
        - $ref: "#/components/parameters/projectIdOptional"
      responses:
        "200":
          description: Ingestion job list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/IngestionJobSummary"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [ingestion]
      summary: Create ingestion job
      operationId: createIngestionJob
      description: Requires `documents:write`, `workspace:admin`, or an `instance_admin` token. Workspace-scoped tokens may only create jobs for projects in their own workspace.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateIngestionJobRequest"
      responses:
        "200":
          description: Created ingestion job
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IngestionJobSummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/ingestion-jobs/{id}:
    get:
      tags: [ingestion]
      summary: Get ingestion job detail
      operationId: getIngestionJobDetail
      description: Requires `documents:read`, `documents:write`, `workspace:admin`, or an `instance_admin` token. Workspace-scoped tokens may only access jobs in their own workspace.
      parameters:
        - $ref: "#/components/parameters/id"
      responses:
        "200":
          description: Ingestion job detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IngestionJobDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/ingestion-jobs/{id}/retry:
    post:
      tags: [ingestion]
      summary: Retry an ingestion job
      operationId: retryIngestionJob
      description: Requires `documents:write`, `workspace:admin`, or an `instance_admin` token. Workspace-scoped tokens may only retry jobs in their own workspace.
      parameters:
        - $ref: "#/components/parameters/id"
      responses:
        "200":
          description: Newly created retry ingestion job detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IngestionJobDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/documents:
    get:
      tags: [documents]
      summary: List documents
      operationId: listDocuments
      security: []
      parameters:
        - $ref: "#/components/parameters/projectIdOptional"
      responses:
        "200":
          description: Document list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentSummary"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [documents]
      summary: Create document metadata record
      operationId: createDocument
      description: Requires `documents:write`, `workspace:admin`, or an `instance_admin` token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDocumentRequest"
      responses:
        "200":
          description: Created document
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/content/ingest-text:
    post:
      tags: [content]
      summary: Queue plain text for ingestion
      operationId: ingestText
      description: |
        Requires `documents:write`, `workspace:admin`, or an `instance_admin` token.

        The current backend enqueues an ingestion job and returns its queued lifecycle state; it does not synchronously create a document/chunk result in this response.
        Workspace-scoped tokens may only queue jobs for projects in their own workspace.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IngestTextRequest"
      responses:
        "200":
          description: Ingestion job accepted and queued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IngestTextResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/content/search-chunks:
    post:
      tags: [content]
      summary: Lexically search chunks within a project
      operationId: searchChunks
      description: Requires one of `documents:read`, `query:run`, `workspace:admin`, or an `instance_admin` token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SearchChunksRequest"
      responses:
        "200":
          description: Matching chunk list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChunkResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/content/embed-project:
    post:
      tags: [content]
      summary: Generate embeddings for project chunks
      operationId: embedProjectChunks
      description: |
        Requires `documents:write`, `workspace:admin`, or an `instance_admin` token.

        If `embedding_model_profile_id` is provided, the backend resolves provider/model from that profile;
        otherwise it uses `provider_kind` and `model_name` directly.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmbedProjectChunksRequest"
      responses:
        "200":
          description: Embedding job result summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedProjectChunksResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/chunks:
    get:
      tags: [documents]
      summary: List chunks by document or project
      operationId: listChunks
      description: Requires `documents:read`, `workspace:admin`, or an `instance_admin` token.
      parameters:
        - $ref: "#/components/parameters/projectIdOptional"
        - $ref: "#/components/parameters/documentId"
        - $ref: "#/components/parameters/limit"
      responses:
        "200":
          description: Chunk list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChunkSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/product/projects/{id}:
    get:
      tags: [product, projects]
      summary: Get product-oriented project overview
      operationId: getProductProjectOverview
      description: Requires one of `documents:read`, `query:run`, `workspace:admin`, or an `instance_admin` token. Workspace-scoped tokens may only access projects in their own workspace.
      parameters:
        - $ref: "#/components/parameters/id"
      responses:
        "200":
          description: Project overview with readiness, query contract, citations, and capability hints
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductProjectOverviewDto"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/product/projects/{id}/diagnostics:
    get:
      tags: [product, projects]
      summary: Get project diagnostics
      operationId: getProductProjectDiagnostics
      description: Requires one of `documents:read`, `query:run`, `workspace:admin`, or an `instance_admin` token. Workspace-scoped tokens may only access projects in their own workspace.
      parameters:
        - $ref: "#/components/parameters/id"
      responses:
        "200":
          description: Project diagnostics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductProjectDiagnosticsDto"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/product/projects/{id}/query-contract:
    get:
      tags: [product, projects]
      summary: Get product query contract description
      operationId: getProductProjectQueryContract
      description: Requires one of `documents:read`, `query:run`, `workspace:admin`, or an `instance_admin` token. Workspace-scoped tokens may only access projects in their own workspace.
      parameters:
        - $ref: "#/components/parameters/id"
      responses:
        "200":
          description: Query contract descriptor
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductQueryContractDto"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/product/documents/{id}:
    get:
      tags: [product, documents]
      summary: Get product-oriented document overview
      operationId: getProductDocumentOverview
      description: Requires `documents:read`, `workspace:admin`, or an `instance_admin` token. Workspace-scoped tokens may only access documents in their own workspace. Usage and cost attribution are currently exposed at project scope because per-document attribution is not persisted yet.
      parameters:
        - $ref: "#/components/parameters/id"
      responses:
        "200":
          description: Document overview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductDocumentOverviewDto"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/product/documents/{id}/diagnostics:
    get:
      tags: [product, documents]
      summary: Get document diagnostics
      operationId: getProductDocumentDiagnostics
      description: Requires one of `documents:read`, `query:run`, `workspace:admin`, or an `instance_admin` token. Workspace-scoped tokens may only access documents in their own workspace.
      parameters:
        - $ref: "#/components/parameters/id"
      responses:
        "200":
          description: Document diagnostics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductDocumentDiagnosticsDto"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/graph-products/{project_id}:
    get:
      tags: [graph, product]
      summary: Get graph product snapshot
      operationId: getGraphProduct
      description: Requires one of `query:run`, `documents:read`, `workspace:admin`, or an `instance_admin` token. Workspace-scoped tokens may only access projects in their own workspace.
      parameters:
        - name: project_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Graph snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphProductResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/graph-products/{project_id}/summary:
    get:
      tags: [graph, product]
      summary: Get graph project summary
      operationId: getGraphSummary
      description: Requires one of `query:run`, `documents:read`, `workspace:admin`, or an `instance_admin` token. Workspace-scoped tokens may only access projects in their own workspace.
      parameters:
        - name: project_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Graph summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphProjectSummaryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/graph-products/{project_id}/diagnostics:
    get:
      tags: [graph, product]
      summary: Get graph project diagnostics
      operationId: getGraphDiagnostics
      description: Requires one of `query:run`, `documents:read`, `workspace:admin`, or an `instance_admin` token. Returns persisted content counts, graph provenance coverage, and concrete readiness blockers/next steps without mutating runtime state.
      parameters:
        - name: project_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Graph diagnostics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphProjectDiagnosticsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/graph-products/{project_id}/search:
    get:
      tags: [graph, product]
      summary: Search graph entities and relations
      operationId: searchGraph
      description: Requires one of `query:run`, `documents:read`, `workspace:admin`, or an `instance_admin` token. Query parameter `q` must be non-empty.
      parameters:
        - name: project_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: q
          in: query
          required: true
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 50
      responses:
        "200":
          description: Graph search results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphSearchResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/graph-products/{project_id}/entities/{entity_id}:
    get:
      tags: [graph, product]
      summary: Get graph entity detail
      operationId: getGraphEntityDetail
      description: Requires one of `query:run`, `documents:read`, `workspace:admin`, or an `instance_admin` token. Workspace-scoped tokens may only access projects in their own workspace.
      parameters:
        - name: project_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: entity_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Graph entity detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphEntityDetailResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/graph-products/{project_id}/entities/{entity_id}/subgraph:
    get:
      tags: [graph, product]
      summary: Get a bounded subgraph around an entity
      operationId: getGraphSubgraph
      description: Requires one of `query:run`, `documents:read`, `workspace:admin`, or an `instance_admin` token. Depth defaults to 1 and must be <= 3.
      parameters:
        - name: project_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: entity_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: depth
          in: query
          required: false
          schema:
            type: integer
            format: int32
            minimum: 0
            maximum: 3
      responses:
        "200":
          description: Graph subgraph response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphSubgraphResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
