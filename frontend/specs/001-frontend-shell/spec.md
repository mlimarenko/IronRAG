# Frontend Shell Spec

## Goal

Provide the first navigable UI shell for RustRAG with routes for workspaces, projects, providers, ingestion, and chat.

## Requirements

- navigation shell exists
- page-level placeholders communicate intent
- API boundary is explicit via shared client boot/module
- structure supports later LightRAG-like query workspace and graph inspector
